spring.application.name=LIFT

# JWT: 시크릿은 32바이트 이상. 비워두면 기동을 중단한다.
bodeum.auth.jwt-secret=${JWT_SECRET:}
bodeum.auth.access-token-ttl=${AUTH_ACCESS_TOKEN_TTL:1h}
bodeum.auth.refresh-token-ttl=${AUTH_REFRESH_TOKEN_TTL:14d}

# OAuth client ids are injected through environment variables in local/production environments.
bodeum.oauth.base-url=${BODEUM_BASE_URL:http://localhost:8080}
# true면 client-id가 비어 있는 provider에 한해 개발용 모의 로그인을 허용한다. 운영에서는 반드시 false.
bodeum.oauth.mock-enabled=${OAUTH_MOCK_ENABLED:false}
bodeum.oauth.kakao.client-id=${KAKAO_CLIENT_ID:}
bodeum.oauth.kakao.client-secret=${KAKAO_CLIENT_SECRET:}
bodeum.oauth.kakao.redirect-uri=${KAKAO_REDIRECT_URI:}
bodeum.oauth.kakao.scope=${KAKAO_SCOPE:}
bodeum.oauth.kakao.token-uri=${KAKAO_TOKEN_URI:}
bodeum.oauth.kakao.user-info-uri=${KAKAO_USER_INFO_URI:}
bodeum.oauth.naver.client-id=${NAVER_CLIENT_ID:}
bodeum.oauth.naver.client-secret=${NAVER_CLIENT_SECRET:}
bodeum.oauth.naver.redirect-uri=${NAVER_REDIRECT_URI:}
bodeum.oauth.naver.token-uri=${NAVER_TOKEN_URI:}
bodeum.oauth.naver.user-info-uri=${NAVER_USER_INFO_URI:}

# 행정안전부_대한민국 공공서비스(혜택) 정보. 서비스키는 서버 환경변수로만 주입한다.
bodeum.public-data.gov24.enabled=${GOV24_PUBLIC_SERVICE_ENABLED:false}
bodeum.public-data.gov24.service-key=${GOV24_PUBLIC_SERVICE_KEY:}
bodeum.public-data.gov24.base-url=${GOV24_PUBLIC_SERVICE_BASE_URL:https://api.odcloud.kr/api/gov24/v3}
bodeum.public-data.gov24.per-page=${GOV24_PUBLIC_SERVICE_PER_PAGE:1000}
bodeum.public-data.gov24.max-pages=${GOV24_PUBLIC_SERVICE_MAX_PAGES:2}
bodeum.public-data.gov24.max-keywords=${GOV24_PUBLIC_SERVICE_MAX_KEYWORDS:5}
bodeum.public-data.gov24.max-results=${GOV24_PUBLIC_SERVICE_MAX_RESULTS:6}

# 토스페이먼츠 테스트 결제. 테스트 키(test_ck/test_sk)로만 데모에서 사용한다.
bodeum.payments.toss.enabled=${TOSS_PAYMENTS_ENABLED:false}
bodeum.payments.toss.secret-key=${TOSS_SECRET_KEY:}
bodeum.payments.toss.base-url=${TOSS_BASE_URL:https://api.tosspayments.com/v1}

# OpenAI는 공공데이터 후보의 설명/우선순위 정리에만 사용한다. 키가 없으면 서버 규칙 fallback으로 동작한다.
bodeum.openai.enabled=${OPENAI_ENABLED:false}
bodeum.openai.api-key=${OPENAI_API_KEY:}
bodeum.openai.base-url=${OPENAI_BASE_URL:https://api.openai.com/v1}
bodeum.openai.model=${OPENAI_MODEL:gpt-5.5}
